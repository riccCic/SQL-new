CREATE TABLE PRODUCT (
    PRODUCT_ID INT,
    NAME VARCHAR(255),
    PRICE DECIMAL(10,2)
);
CREATE TABLE INVENTORY (
    PRODUCT_ID INT,
    STORE_ID INT,
    QUANTITY INT
);

1. LEFT JOIN tra PRODUCT e INVENTORY sulla colonna PRODUCT_ID:
SELECT *
FROM PRODUCT P
LEFT JOIN INVENTORY I
ON P.PRODUCT_ID = I.PRODUCT_ID;

2. RIGHT JOIN tra PRODUCT e INVENTORY sulla colonna PRODUCT_ID:
SELECT *
FROM PRODUCT P
RIGHT JOIN INVENTORY I
ON P.PRODUCT_ID = I.PRODUCT_ID;

3. INNER JOIN tra PRODUCT e INVENTORY sulla colonna "PRODUCT_ID":
SELECT *
FROM PRODUCT P
INNER JOIN INVENTORY I
ON P.PRODUCT_ID = I.PRODUCT_ID;

4. LEFT JOIN tra PRODUCT e INVENTORY sulla colonna "PRODUCT_ID" con la condizione che non ci sia corrispondenza in INVENTORY:
SELECT *
FROM PRODUCT P
LEFT JOIN INVENTORY I
ON P.PRODUCT_ID = I.PRODUCT_ID
WHERE I.PRODUCT_ID IS NULL;

5. RIGHT JOIN tra PRODUCT e INVENTORY sulla colonna "PRODUCT_ID" con la condizione che non ci sia corrispondenza in PRODUCT:
SELECT *
FROM PRODUCT P
RIGHT JOIN INVENTORY I
ON P.PRODUCT_ID = I.PRODUCT_ID
WHERE P.PRODUCT_ID IS NULL;
